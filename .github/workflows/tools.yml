# Github workflow to build and test the native Nexus Tools code

name: Native Nexus Tools
on:
  pull_request:
    paths:
      - "tools/**"
      - ".github/workflows/tools.yml"
  push:
    branches:
      - main
    paths:
      - "tools/**"
      - ".github/workflows/tools.yml"

# Fix for OOM.
env:
  CARGO_BUILD_JOBS: 1
  CARGO_INCREMENTAL: 0
  RUSTFLAGS: >
    -C codegen-units=1

jobs:
  # 1. Install Rust
  # 2. Install Rust cache
  # 3. Install just
  # 4. Build, fmt, clippy and test the native Nexus Tools
  build-and-test:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      # 1.
      - name: Install Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable, nightly
          override: true
          components: rustfmt, clippy

      # 2.
      - name: Cache Build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: tools

      # 3.
      - uses: taiki-e/install-action@just

      # 4
      - run: cargo +stable --version
      - run: cargo +nightly --version
      - run: just tools check
      - run: just tools fmt-check
      - run: just tools clippy
      - run: just tools test
